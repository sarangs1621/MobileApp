# Dummy values so `prisma validate` / `prisma generate` resolve env without secrets.
# Real connection strings come from the root .env (gitignored). prisma validate
# does not connect — it only checks the schema + that the variable is defined.
DATABASE_URL="postgresql://user:password@localhost:5432/school_portal?schema=public"
