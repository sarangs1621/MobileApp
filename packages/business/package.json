{
  "name": "@repo/business",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "bootstrap": "dotenv -e ../../.env -- tsx scripts/bootstrap-school.ts",
    "provision": "dotenv -e ../../.env -- tsx scripts/provision-user.ts",
    "verify:auth": "dotenv -e ../../.env -- tsx scripts/verify-auth.ts"
  },
  "dependencies": {
    "@repo/auth": "workspace:*",
    "@repo/constants": "workspace:*",
    "@repo/core": "workspace:*",
    "@repo/db": "workspace:*",
    "@repo/notifications": "workspace:*",
    "@repo/types": "workspace:*"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "tsx": "^4.19.2"
  }
}
