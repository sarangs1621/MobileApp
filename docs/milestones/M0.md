# M0 — Foundation & Project Setup — ✅ COMPLETE / FROZEN

Tag: `v0.1.0-foundation`.

## Delivered
Turborepo + pnpm workspaces; 12 packages (`api, auth, business, constants, core, db, i18n, notifications, types, ui, utils, validation`); `apps/web` (Next 15) + `apps/mobile` (Expo) shells with health check; Prisma infra (model-free schema, client, seed, repositories); Supabase client factories; tRPC factory/context/middleware; UI tokens + Tailwind preset; en/ml i18n; notification interfaces; strongly-typed env; ESLint (import boundaries) + Prettier + Husky + lint-staged + commitlint; Vitest + Playwright config; GitHub Actions CI; README.

## Validation
typecheck · lint (+ boundary negative tests) · tests · `prisma validate` · web build + live `/api/health` · expo export · CI pipeline (locally). All green.

## Key decisions
Node 24 LTS (nvm); `pnpm.overrides` pin React 19.2.3 (single instance); app-enforced authz (RLS defense-in-depth); repositories over raw Prisma; partial-index idiom; single-tenant `schoolId`; feature flags.

## Risks / notes
`@db.Date` for calendar columns (decided in v1.3); real Supabase/EAS/store accounts pending; bleeding-edge stack (Expo 56 / RN 0.85 / Next 15.5).

**Frozen** — read-only.
