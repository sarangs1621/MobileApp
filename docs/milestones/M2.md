# M2 — People + Bulk Import + Academic Structure — ⬜ NOT STARTED

Per Dev PRD v1.3 §13 (renumbered — REVIEW_FINDINGS A1). Begins after M1 is complete + approved.

## Planned scope
- People: `Student`, `Guardian`, `Staff` records; guardian↔student M:N (one `isPrimary`); `User`↔`Staff`/`Guardian` relations.
- Bulk import (CSV/Excel): column mapping, validation, downloadable error report, `ImportJob`, partial success.
- Academic structure: `AcademicYear` (one-current invariant), `ClassLevel`, `Division`, `Subject`, `ClassSubject`, `TeacherAssignment`, class-teacher flag; enrollment.

## Depends on
Authentication (**frozen**) — provisioning/seed should be in place; scope predicates (division/guardian/student ownership) plug into the M1 `ScopeRule` extension point.

## Pending decisions (from v1.3 open questions / REVIEW_FINDINGS)
`Holiday` model (B1) · typed `SchoolSettings` (B4) · staff-row provisioning invariant (B3) · duplicate-phone import rule (B8) · subject list + practicals ([CONFIRM]).

## Steps / validation
To be defined at M2 kickoff (same 11-step workflow).
