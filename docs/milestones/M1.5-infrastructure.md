# M1.5 — Infrastructure Provisioning — ✅ COMPLETE

Deployment & environment configuration only (no M2 features). Date: 2026-07-05.
Live project: Supabase `wupcsvbyrknfuuskzuzp` (ap-southeast-1, free plan).
Runbook (repeatable for staging/prod): `docs/RUNBOOK_SUPABASE_SETUP.md`.

## Delivered

- **Live Supabase project wired** — root `.env` filled (URL/anon/service-role/session-pooler `DATABASE_URL`); schema migration `20260703000000_init_auth` applied with `prisma migrate deploy`.
- **Admin provisioning module** — `@repo/auth` `createAdminClient` + `adminCreateUser`/`adminFindUserId`/`adminDeleteUser` (+ `createHeadlessClient`, `restoreSession`); all Supabase calls stay in `@repo/auth`.
- **Ops scripts** (`packages/business/scripts/`, run via `pnpm --filter @repo/business run …`):
  - `bootstrap` — School row + super-admin auth user + SUPER_ADMIN profile (INVITED) + `USER_PROVISIONED` audit, idempotent.
  - `provision` — single-user Admin-API provisioning (M1 decision D3), staff email+password or parent phone, audited, idempotent.
  - `verify:auth` — live evidence suite (below).
- **Auth security config applied via Management API** (config-as-code; PAT since revoked): signups disabled, phone provider + test OTP (`+919999900001` → `123456`, valid until 2027-12-31), SMS OTP expiry **600 s** / 6 digits, password min length **10**, site URL + `/update-password` redirect allowlist.
- **SchoolRepository** added to `@repo/db` (standalone; `Repositories` aggregate untouched).

## Evidence — live verification (`pnpm --filter @repo/business run verify:auth`)

All 11/11 checks passed against the real backend (2026-07-05):

| Check | Result |
|---|---|
| database-connectivity | PASS — Prisma via session pooler |
| public-signup-disabled | PASS — `settings.disable_signup=true`; probe rejected `422 signup_disabled` |
| otp-rejects-unknown-phone | PASS — unprovisioned phone rejected |
| email-login | PASS — super-admin signed in (uid `552cadf2…`) |
| super-admin-bootstrap | PASS — Principal role=SUPER_ADMIN from DB |
| activation-flow | PASS — INVITED → ACTIVE, idempotent re-run |
| session-restoration | PASS — tokens adopted by fresh client, verified server-side |
| token-refresh | PASS — access token rotated |
| logout | PASS — session cleared |
| otp-login | PASS — test OTP verified for `+919999900001` (uid `68f32fae…`) |
| parent-provisioning | PASS — Principal role=PARENT, activated |

## Environment verification

- Web: `next build` **with real env validation** (no `SKIP_ENV_VALIDATION`) ✓
- Mobile: `expo export --platform android` with real `EXPO_PUBLIC_*` ✓ (native-only app — static *web* export crashes on SecureStore by design, not a regression)
- Full suite after all changes: **tests 7 suites / 90 total** ✓ · lint 14/14 ✓ · typecheck 14/14 ✓

## Deferred / follow-ups

- **Real SMS provider** — Twilio creds are placeholders; only the test OTP number works. Pick provider + India DLT before parent go-live; remove test OTP.
- **Leaked-password protection (HIBP)** — Pro-plan-only (402 on free tier); enable on upgrade. [SECURITY_REVIEW_M1 §4, partial]
- **Custom SMTP** — built-in Supabase email (rate-limited) fine for dev; configure before production.
- **Credential rotation** — service-role key + DB password were shared in chat during setup; rotate both (and the seed admin password) before any real data. PAT used for config was single-purpose; revoke it.
