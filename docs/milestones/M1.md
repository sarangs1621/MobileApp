# M1 — Authentication & User Profiles — ✅ COMPLETE / FROZEN (approved 2026-07-05)

Scope: auth · authorization · user profiles · sessions. Out: students/attendance/exams/homework/fees/notifications/CRUD.

## Steps
- ✅ 1 — Requirements analysis
- ✅ 2 — Auth DB design: `School`, `User`, `DeviceToken`, `AuditLog` + enums (`Role`/`UserStatus`/`Locale`); init migration
- ✅ 3 — Authorization design: permission (`assertCan`) **separate from** scope (`ScopeRule` + `assertScope`); errors in core
- ✅ 4 — Auth architecture: Supabase clients; `getAuthUser` (cookie+bearer); `resolvePrincipal`; `activateUser`; `Principal` = {userId, schoolId, role, status}; `onboardingProcedure`
- ✅ 5 — API: `auth.me`, `auth.registerProfile`
- ✅ 6 — Business: `updateProfile`, `setRole`, `disableUser`, `enableUser`; `AuditLog` repo + `withTransaction`; `DomainError→TRPCError`
- ✅ 6.1 — Refinement: activation audited (`USER_ACTIVATED`, in-transaction)
- ✅ 7 — Mobile auth: splash/gate/login/OTP/session/logout/role shell (`expo export` ✓)
- ✅ 8 — Web auth: login/forgot/OTP/update-password/protected layout/refresh middleware/logout/dashboard (`next build` ✓)
- ✅ 9 — Security review (`docs/SECURITY_REVIEW_M1.md`: 2 findings fixed; Supabase dashboard checklist pending provisioning)
- ✅ 10 — Tests: auth (`getAuthUser` cookie/bearer, session helpers incl. `shouldCreateUser:false` regression), authz/permission, route protection (API gates + web middleware/protected layout), session, input/edge cases. **Caught+fixed critical bug:** `mapDomainErrors` never fired (tRPC v11 `next()` returns a result, doesn't throw) — business `DomainError`s surfaced as 500s instead of FORBIDDEN/NOT_FOUND (`packages/api/src/trpc.ts`).
- ✅ 11 — Documentation: `API_INVENTORY.md` auth section (implemented state, gates, all 6 procedures), `API_CONVENTIONS.md` §6 (DomainError mapping + tRPC v11 `next()` nuance), `features/authentication.md` (sessions/revocation, security & tests), status/memory/milestone docs synced. No new ADR (no new decision).

## Validation (current)
typecheck 14/14 · lint 14/14 · tests 7 suites, 80 total (auth 22, business 20, api 14, core 8, web 7, validation 6, utils 3) · web build · expo bundle.

## Architecture decisions (M1)
Centralized permission catalog (`ROLE_PERMISSIONS`) + `can`; `Principal` carries schoolId/status; permission vs scope split w/ extension point; **no transport role gate**; role/status from DB only; audit-in-transaction; Supabase calls only in `@repo/auth`; lazy web browser client; mobile imports `AppRouter` type-only.
Decisions D1–D5: **B** (auth + admin lifecycle) · `getUser()` verification (local-JWKS deferred) · single-user Admin-API provisioning · mocked/dev OTP for now · AuditLog introduced.

## Known risks
Provisioning + seed + SMS provider pending (blocks real sign-in/OTP). No live Supabase in dev → flows verified structurally + mocked unit tests. Config artifacts (`package-lock.json`, root `tsconfig.json`) to clean up.
